<% provide(:title, "Forgot password") %>
<h1 align="center">Forgot password</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(:password_reset, url: password_resets_path) do |f| %>
      <%= f.label :email %>
      <%= f.email_field :email, placeholder: "Enter your email", class: 'form-control' %>

      <%= f.submit "Submit", class: "btn btn-primary btn-top-margin" %>
    <% end %>
    After you click Submit. Check your email for a password reset link from john@westcompany.org, If you dont see an email from us please check your spam folder
  </div>
</div>
