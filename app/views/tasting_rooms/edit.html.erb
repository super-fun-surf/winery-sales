<% provide(:title, "Edit Tasting Room") %>

<div class="row">
  <div class="col-md-2 col-lg-3 col-xl-4"></div>
  <div class="col-md-8 col-lg-6 col-xl-4">
    <div class="card card-block">
      <h3>Editing <string><%= @tasting_room.winery.name %> @ <strong><%= @tasting_room.region.name %></strong></h3>
      <%= render 'form', tasting_room: @tasting_room %>
    </div>
  </div>
</div>
