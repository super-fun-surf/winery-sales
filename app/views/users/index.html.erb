
<%# link_to 'signup', new_user_url %>
<br>
<%= will_paginate @users %>

<ul class="users">
<% @users.each do |user| %>
  <li>
    <%= gravatar_for user.email %>
    <%= link_to user.name, user %>
  </li>
<% end %>
</ul>

<%= will_paginate @users %>
