<%= form_for(sales_summary) do |f| %>
  <% if sales_summary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sales_summary.errors.count, "error") %> prohibited this sales_summary from being saved:</h2>

      <ul>
      <% sales_summary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tasting_room_id %>
    <%= f.text_field :tasting_room_id %>
  </div>

  <div class="field">
    <%= f.label :num_of_tasters %>
    <%= f.number_field :num_of_tasters %>
  </div>

  <div class="field">
    <%= f.label :num_of_purchasers %>
    <%= f.number_field :num_of_purchasers %>
  </div>

  <div class="field">
    <%= f.label :num_of_club_signups %>
    <%= f.number_field :num_of_club_signups %>
  </div>

  <div class="field">
    <%= f.label :sales_in_dollars %>
    <%= f.text_field :sales_in_dollars %>
  </div>

  <div class="field">
    <%= f.label :year %>
    <%= f.number_field :year %>
  </div>

  <div class="field">
    <%= f.label :month %>
    <%= f.number_field :month %>
  </div>

  <div class="field">
    <%= f.label :day %>
    <%= f.number_field :day %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
