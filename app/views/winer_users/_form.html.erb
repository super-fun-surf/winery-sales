<%= form_for(winer_user) do |f| %>
  <% if winer_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(winer_user.errors.count, "error") %> prohibited this winer_user from being saved:</h2>

      <ul>
      <% winer_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :winery_id %>
    <%= f.text_field :winery_id %>
  </div>

  <div class="field">
    <%= f.label :permissions %>
    <%= f.number_field :permissions %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
