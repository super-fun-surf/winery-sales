<% if donkey.errors.any? %>
  <!--div id="error_explanation"-->
    <div class="alert alert-danger">
      The form contains <%= pluralize(donkey.errors.count, "error") %>.
      <ul>
      <% donkey.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>

  <!--/div-->
<% end %>
