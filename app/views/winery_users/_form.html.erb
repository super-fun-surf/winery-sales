<%= form_for(winery_user) do |f| %>
  <% if winery_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(winery_user.errors.count, "error") %> prohibited this winery_user from being saved:</h2>

      <ul>
      <% winery_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :winery_id %>
    <%= f.text_field :winery_id %>
  </div>

  <div class="field">
    <%= f.label :position %>
    <%= f.text_field :position, placeholder: 'Job Title i.e. CFO' %>
  </div>


  <div class="field">
    <%= f.label :permissions %>
    <%= f.number_field :permissions %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
